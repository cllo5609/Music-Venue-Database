{{!
    Authors:    Charles D. Maddux
                Clinton Lohr
    Date:       7 Aug 2022
    Rev Date:   7 Aug 2022
    Description: Artist Needs Instruments page - creates the structure and includes descriptions for the artist_needs_ins webpage
    Sources: https://github.com/osu-cs340-ecampus/nodejs-starter-app // Code for the entry delete, 'displayAddForm' and 'displayUpdateForm'
    buttons derived from this source
}}

<div class="page-title">
    <h1>Requested Instruments</h1>
</div>
<div class="container">
    <div class="content-body">
        <div class="page-desc">
            <h2>Here you can add, delete or update the details of an instrument request by an upcoming performer(s).</h2><br>
            <h3>Don't see the artist/group or instrument you're looking for in the form dropdown menu?</h3>
            <h3>Use the 'Artists' page to add a new artist to the database or use the 'Instruments' page</h3>
            <h3>to add a new instrument.</h3><br>
        </div>
        <br>

        {{! Displays Artist Needs Instruments table}}
        <h2><b>Artist Instrument Requests</b></h2>
        <table class="sharp-table">
            {{!-- Table Header section --}}
            <thead >
                <tr>
                    {{#each data.[0]}}
                        <th>
                            {{@key}}
                        </th>
                    {{/each}}
                    <th>Remove</th>
                </tr>
            </thead>
            {{!-- Table Body Section --}}
            <tbody class="table-body">
                {{#each data}}
                    {{!-- SELECT Artists.artist_id AS 'ID' Artists.name AS 'Band', Instruments.instrument_type AS 'Gear'; --}}
                    <tr>
                        <td>{{this.ID}}</td>
                        <td>{{this.Band}}</td>
                        <td>{{this.Gear}}</td>
                        <td><button onclick="deleteRequest({{this.ID}})"class="delete-button">Delete</button></td>  {{! button and function to delete table data}}
                    </tr>
                {{/each}}
            </tbody>
        </table>
        <br>

        {{! Form to add a new entry to the database}}
        <button class="display-form-button" onclick="displayAddForm()">Click here to request an instrument</button> {{! function call to display/hide form on-click}}
        <div class="form-body" id="show-add">
            <form class="sharp-form" id="add-request-form">
                <div class="form-title">Add a Request</div>
                <p class="form-desc">
                    Select the name of the artist/group and the instrument they are <br>
                    requesting using the dropdown boxes below, then hit submit.
                </p>
                {{! Artist name form field}}
                <div class="form-item">
                    <label class="form-label" for="add-name">Artist Name: </label>
                    <select class="form-dropdown" name="add-name" id="add-name">
                        <option value="test">Select Artist</option>
                        {{#each data2}}
                            {{! Dynamically populated dropdown menu for artist name}}
                            <option class="form-input" value="{{this.name}}">{{this.name}}</option>
                        {{/each}}
                    </select>
                </div>
                {{! Instrument type form field}}
                <div class="form-item">
                    <label class="form-label" for="add-inst">Instrument: </label>
                    <select class="form-dropdown" name="add-inst" id="add-inst">
                        <option value="test">Select Instrument</option>
                        {{#each data3}}
                            {{! Dynamically populated dropdown menu for instrument type}}
                            <option class"form-input" value="{{this.instrument_type}}">{{this.instrument_type}}</option>
                        {{/each}}
                    </select>
                    <input class="form-button" type="submit">
                </div>
            </form>
        </div>

        {{! Form to update an existing entry in the database}}
        <button class="display-form-button" onclick="displayUpdateForm()">Click here to update an existing request</button> {{! function call to display/hide form on-click}}
        <div class="form-body" id="show-update">
            <form class="sharp-form" id="update-request-form">
                <div class="form-title">Update a Request</div>
                <p class="form-desc">
                    Select the ID of the entry that you wish to edit using the dropdown menu below.
                    Add the artist and instrument they are requesting, then hit submit.
                </p>
                {{! Request id form field}}
                <div class="form-item">
                    <label class="form-label" for="input-id">Request ID: </label>
                    <select class="form-dropdown" name="input-id" id="input-id">
                        <option value="test">Select an ID</option>
                        {{#each data}}
                            {{! Dynamically populated dropdown menu for request id}}
                            <option class="form-input" value="{{this.ID}}">{{this.ID}}</option>
                        {{/each}}
                    </select>
                </div>
                {{! Artist name form field}}
                <div class="form-item">
                    <label class="form-label" for="update-name">Artist Name: </label>
                    <select class="form-dropdown" name="update-name" id="update-name">
                        <option value="test">Select Artist</option>
                        {{#each data2}}
                            {{! Dynamically populated dropdown menu for artist name}}
                            <option class="form-input" value="{{this.name}}">{{this.name}}</option>
                        {{/each}}
                    </select>
                </div>
                {{! Instrument type form field}}
                <div class="form-item">
                    <label class="form-label" for="update-inst">Instrument: </label>
                    <select class="form-dropdown" name="update-inst" id="update-inst">
                        <option value="test">Select Instrument</option>
                        {{#each data3}}
                            {{! Dynamically populated dropdown menu for instrument type}}
                            <option class="form-input" value="{{this.instrument_type}}">{{this.instrument_type}}</option>
                        {{/each}}
                    </select>
                </div>
                <div class="form-item">
                    <input class="form-button" type="submit">
                </div>
            </form>
        </div>
    </div>
</div>

{{! Reference to JavaScript file for Artist Needs Instruments entity}}
<script src="./js/mod_artist_needs_ins.js" async></script>